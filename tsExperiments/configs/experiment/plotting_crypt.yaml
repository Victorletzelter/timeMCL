# @package _global_

data:
  save_path: "${oc.env:PROJECT_ROOT}/tsExperiments/data/"

  crypto_tickers:
      - "BTC-USD"  # Bitcoin
      - "ETH-USD"  # Ethereum
      - "XRP-USD"  # Ripple
      - "LTC-USD"  # Litecoin
      - "BCH-USD"  # Bitcoin Cash
      - "ADA-USD"  # Cardano
      - "DOGE-USD" # Dogecoin
      - "SOL-USD"  # Solana
      - "LINK-USD" # Chainlink
      - "MATIC-USD" # Polygon
      - "XLM-USD"  # Stellar
      - "ETC-USD"  # Ethereum Classic
      - "XMR-USD"  # Monero
      - "EOS-USD"  # EOS
      - "VET-USD"   # VeChain

  num_batches_per_epoch: 20
  num_batches_val_per_epoch: 1
  batch_size: 64
  shuffle_buffer_length: Null
  start_date: "2023-07-01" #global start date of the stock market data 
  end_date: "2025-03-01" #global end date 
  train:
    type: "financial_data"
    split_train_val: true
    n_pred_steps_val: 10 # number of step of validation (as a factor of the prediction length)
    num_feat_dynamic_real: 4
    start_date: "2023-07-01" #start date for the train 
    end_date: "2024-09-01" #end date for the train 
  valid:
    start_date: "2024-09-02" #start date for the train 
    end_date: "2024-12-05" #end date for the train 
  test:
    start_date: "2024-12-06"
    end_date: "2025-03-01"
    num_tests: 4 

trainer:
  max_epochs: 1

run_name: "crypt_setup"

task_name: "setup"

logger:
  mlflow:
    experiment_name: "setup"

train: False
test: False

visualize_specific_date: True
date_of_pred: "2024-12-20"
start_date_viz: "2024-12-05"
end_date_viz: "2025-03-01"